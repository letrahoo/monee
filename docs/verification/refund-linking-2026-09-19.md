# M-08 退款验证

基线 main `9c04177`，分支 `feat/refund-linking`，[PR #13](https://github.com/letrahoo/monee/pull/13)。迁移基础提交 `f176245`，最终实现 `9d5e63f`；以下最新收尾优先于历史未完成记录。

## 最终补验（2026-09-19）

- Mac 重新通过显式测试域名配置启动，恢复独立合成账本，九月毛额300/退款80/净支出220/收入0、6笔，与先前一致。候选容器已重启，实际 schema 3 health 正常；未操作原账本。
- 输入工具恢复：对累计已退100元的 `SYN-M08-Mac-Refund` 输入更正金额99、合成原因并点击“查看更正影响”，明确拒绝“已有退款关联，不能改为收入或将消费金额改到已退款金额以下”；取消后未修改账目。上轮剪贴板问题不再阻塞。
- Mac 在账号设置主动退出后显示 Google/GitHub 登录页；退出进程并以同一域名、CA、独立数据目录重启，仍显示登录页，不恢复被撤销会话。随后关闭测试客户端。
- `2d691a9` 六项 GitHub 检查通过：[三平台构建](https://github.com/letrahoo/monee/actions/runs/35422468705)、[Compose/镜像](https://github.com/letrahoo/monee/actions/runs/35422468707)。[GitHub Codex 审查](https://github.com/letrahoo/monee/pull/13#issuecomment-5739489794)明确未发现重大问题，与独立 Agent 结论一致。本轮仅补文档，不改变已验收实现；合并前仍核对最新提交 CI。
- 两处 Monee 上游恢复原4173/schema 2，校验/reload通过；LAN/Tailnet health实例一致、HTTP308、匿名/无效Bearer受保护接口401，resume/Hermes401。保留Bearer修复和用户取消的Monee外层密码；合成候选卷保留，未试写真实账本或生产部署。

双端主流程、异常输入、权限撤销、重复操作/丢响应恢复、统计一致性及退款关联保护的必需验收已完成。更复杂的“不确定请求期间降权再恢复”组合仅有自动测试/审查，不宣称双端实测；未确认请求跨整页刷新/进程退出持久化、原生退款自动匹配、转账/还款和跨来源合并不在本次交付范围。

## 最新续验：Mac HTTPS 与双端丢响应恢复

2026-09-19，Mac 分发包基于 `9d5e63f`，显式 HTTPS 测试模式、独立数据目录与受信任公共 CA。域名临时指向既有 schema 3 隔离候选卷（镜像来源见下文），不访问原 schema 2 真实账本。

- Mac 实际完成 Google OAuth、桌面 proof 领取、Bearer 经网关读取及写入；没有复制浏览器 Cookie 或会话。创建独立 `M08 Mac Synthetic 0919`。
- 手工合成消费 `SYN-M08-Mac-Refund` 100 元，退款 30 后九月毛额100/退款30/净支出70/收入0；零金额与三位小数无法提交；再退70.01被拒绝。余下70在十月退回，十月毛额0/退款70/净支出-70/收入0，九月不变。关联记录累计100/剩余0，不再显示新增退款表单。
- 退出 Mac 进程再启动，同一隔离配置自动恢复 Google 登录和上述数据；随后 Web 独立 Google 登录核对同一账本和统计一致。这里仅验证已确认会话/数据持久化，不宣称 pending 请求跨进程保留。
- 合成消费 `SYN-M08-LostResponse` 200 元：通过现有网关仅匹配候选 `POST /api/v1/transactions/*/refunds`，上游201后使用临时 `handle_response` / `abort` 丢弃成功响应。Web 25元、Mac 15元请求均观察到连接失败/待确认，但另端及轮询已见落账。临时规则随即移除。
- Mac 关闭面板→“继续确认上次退款”→重试成功，15元仅一笔；另一次 Web 10元断线后，关闭面板并切换至账本列表、重新进入，待确认原金额10仍保留，重试后仅一笔。最终该消费退款25+15+10=50，九月全账本6笔、毛额300/退款80/净支出220/收入0，Mac 与 Web一致。首次Web25元的待确认状态在切换工具后未保留，未确认是否整页重载，因此不将该次计作Web重试通过；以上另一次10元完整操作作为确定性证据。
- 所有故障规则均已移除，没有加入应用代码、禁用认证或使用第二个代理。网关操作依据 [Caddy response interception](https://caddyserver.com/docs/caddyfile/directives/reverse_proxy#intercepting-responses) 与 [abort](https://caddyserver.com/docs/caddyfile/directives/abort)。
- 独立 Agent 再次定向复查 `9d5e63f` 退款幂等/逐请求权限/作用域与 Mac HTTPS，会话与重定向边界未发现确认 P1/P2。

进一步实测：

- 经用户在本次操作时明确授权，将现有 Google 身份加入既有 `M08 Refund Synthetic` 合成账本，角色只读→可编辑→只读→移除。Mac 只读时记账/导入禁用，尚有70元可退的消费仅能查询；可编辑时写入入口恢复。独立 Web Google 会话经用户本人复核后进入同账本，降权使正在打开的退款表单卸载，重新打开仅可查记录，无输入与保存。最后移除成员，Mac 和 Web 均自动回到账本列表，该账本消失。新增成员已移除，未扩大真实账本或其他服务权限。
- Mac 可编辑期间实际撤销原三笔合成导入：预览保留两个有退款关联的消费，仅撤销50元收入；确认后九月收入0、净支出0、3笔。恢复预览只恢复该收入；确认后收入50、净支出0、4笔，无重复退款。
- Web 对0和1.001退款输入，实际截图确认保存按钮禁用；先前自动测试不再是此两项唯一证据。
- Mac 后续深层更正表单的文字输入遇到操作工具剪贴板超时，滚动接口报无窗口；AX查询和按钮点击仍可操作。重绑窗口及重启隔离客户端未解决，未将100→99更正用例计为通过，已请求用户将 App 切到前台后再试。不能将其误报为 OAuth、Caddy 或退款规则错误。

仍待补：Mac 更正金额保护的实际输入，以及退出/重启退出态验证；最终证据/审查与提交检查收尾。M-08 保持待验收，PR 保持草稿。更复杂的“不确定请求期间降权再恢复”组合场景当前只有自动测试/审查，不宣称已经双端实测。

## 迁移基础实现与通过项

- schema 3：退款类型和同账本原交易外键；逐列复制旧交易，保留稳定 ID/来源/分录/审计。
- 升级前一致性快照、备份验证失败停止升级、迁移事务失败回滚；外键开关恢复的错误向上返回。
- 统一 schema 版本，身份初始化不降级，恢复工具拒绝未来 schema 且不生成目标文件。
- `go -C server test -race ./...`、`go -C server vet ./...` 通过（包括本轮补充测试）。
- 合成测试覆盖旧库来源/分录/金额/版本、身份/准入/成员/会话、创建人和软删除逐列保留、坏外键回滚、备份失败不变更原库、旧快照恢复、同账本关系与未来版本拒绝。
- 独立审查仅覆盖迁移基础，未发现确认 P1/P2。建议的身份/会话、软删除/创建人、备份失败与未来恢复测试已补充。
- 共享 JVM 测试、Web 生产构建、Mac 分发构建通过。客户端源代码未变化，部分任务复用有效缓存；内置 Go 服务与 Mac 分发资源已更新。本机最初默认 Java 缺失，再用 Homebrew JDK 时打包供应商检查失败，随后使用现有 Temurin JDK 17.0.20.1 重试成功，未禁用打包检查。

## 历史未完成记录（已由最终补验更新）

- Mac 更正金额保护及退出/重启退出态验证。退款主流程、跨月、运行中丢响应重试、双端只读/撤权、零/精度输入和Mac撤销/恢复关联保护已补验；不能将自动测试覆盖等同双端实测。
- 补验或修复后的最终审查和最新提交 GitHub CI。
- 迁移阶段提交 `802980d` 的三平台构建 `35383553284` 和容器联调/镜像 `35383553314` 六项检查通过；不能将它们等同后续功能提交检查。

## 退款功能自动验证

- Go 全量 race/vet 通过；退款领域和 HTTP 测试覆盖同月/跨月/全额/多次、零负数精度币种日期/超额、同键重试和跨操作冲突、共享锁及独立 SQLite 连接并发、原消费版本、原交易类型/删除、平衡分录、持久化和新版快照恢复。
- HTTP 验证匿名 401、viewer 写入 403/查询允许、其他账本 404/非成员 403、编辑后降权再重试 403、恶意 Origin 403、Cookie 写入缺失/错误 CSRF 403 与正确 proof 成功。
- 有关联时更正/分类/导入撤销保护通过；退款备注可改，不允许单独改关联分类。
- 共享 UI 已实现登记、关联明细与净额显示；图表以毛额为分母，退款月/零毛额不除零。新增共享测试覆盖解码/整数金额、请求原版本/内容/key保留、服务/用户/账本隔离与错误请求不能清除现有状态。
- 独立后端审查未发现 P1/P2；UI 审查发现 pending 仅放页面会在上层网络故障卸载时丢失，现已提升到 App 持有的 LedgerApi，并经复核确认解决。范围限当前运行客户端；整页重载或进程退出不会持久化未确认请求，不能声称覆盖该场景。
- 本轮 Go 全量 race/vet、新增共享 JVM 测试、Web 生产构建、Mac 分发构建均通过；修复 pending 持有层后已重建。Wasm 包体积/动态依赖的既有 warning 仍在；构建绿灯不替代未完成的双端实际操作。

以上自动测试仅使用临时合成数据，未升级或试写实际用户数据库。

## 2026-09-19 Web 实测与 Mac 回调阻塞

功能提交 `108116c331fc20c693caf89f4177d4fd8246380d` 六项 GitHub 检查全部成功：[三平台构建](https://github.com/letrahoo/monee/actions/runs/35387283559)、[Compose 与 API/Web 镜像](https://github.com/letrahoo/monee/actions/runs/35387283583)。独立功能审查和 pending 修复复查无未解决 P1/P2；PR 仍为草稿。

验收环境：本机 ARM64、Chrome 真实 GitHub OAuth、独立容器数据卷，schema 3。API 从上述功能提交按 ARM64 构建；Web 使用同提交本机已通过构建的 Wasm 配合仓库 Nginx 配置，因此不是 CI AMD64 镜像的字节等价验收。实际加载 API 镜像 `sha256:87e5fe76573052bdb96df6b2e3525cc4b5582e42ccc86dccef0ebb4fd4a52661`、Web 镜像 `sha256:dd1188455f7ee4d218be1a60559d93119a3cc6d586ea076c5387ac5bd5ab2c9b`。

通过 UI 新建 `M08 Refund Synthetic`，导入三条合成记录：9 月支出 100、8 月支出 100、9 月收入 50。实际观察：

- 9 月消费退 30 后，毛额 100、退款 30、净支出 70；收入仍为 50。退款详情明确不计收入，可返回原消费；显示剩余可退 70。
- 再退 70.01 被拒绝，金额/条数不变；退 70 成功后累计 100、净支出 0、收入仍 50。关联页显示两笔退款和剩余额度 0，不再提供追加表单。
- 8 月消费于 10 月退 30：原月净支出仍 100，10 月毛额 0、退款 30、净支出 -30、收入 0；只有退款的分类正常显示负净额。
- 重启候选 API 后重载浏览器，会话与独立账本仍可读取；9 月两笔退款及净支出 0/收入 50 保留。
- 已全退原消费更正为 99 时被后端拒绝，提示不得低于已退款额；原账目保持不变。
- 撤销三笔导入的预览明确保留两个已关联退款的消费，仅撤销收入；实际确认后退款和原消费保留，收入降为 0。恢复预览/确认只恢复该收入，收入回到 50，退款不重复。
- 匿名账本请求仍为 401。

Mac 使用同提交本机分发包与独立空数据目录，在回环端口 4278 启动随包服务；discovery/health 实例一致、schema 3，实际进入登录页。GitHub 返回 `Invalid Redirect URI`，Google 返回 `redirect_uri_mismatch`，均未登记该独立端口，故没有完成 Mac 登录或退款验收。没有复制会话、伪造 discovery、修改 OAuth 检查或停止原 4173 服务。已向用户请求允许在现有 GitHub App 新增 `http://127.0.0.1:4278/auth/callback/github`（保留其他回调、密钥和权限），等待明确回应，不自行修改认证配置。

收尾：仅在 Web 验收期间临时将 Monee 的现有 Caddy 上游切到候选端口；已恢复原 4173 服务并核对原 schema 2 实例，Caddy 校验通过。Monee 外层密码仍取消，resume/Hermes 匿名仍 401。Mac 测试 App 和其独立服务已停止，合成测试数据保留供接续；原服务、真实账本、其他认证和生产部署未变。

## 2026-09-19 后续回归与界面工具阻塞

- 文档提交 `246bf24` 的六项检查现已全部成功：[三平台构建](https://github.com/letrahoo/monee/actions/runs/35391236624)、[Compose 与镜像](https://github.com/letrahoo/monee/actions/runs/35391236703)。不代替此后新增测试提交的 CI。
- 续验时浏览器工具拒绝打开候选 URL，明确报告管理员强制策略无法验证。没有用原生浏览器操作、其他工具或代理绕过，未新增 Web 通过项；临时路由立即恢复原 4173/schema 2 实例并通过 Caddy 校验。Mac 回调仍等待用户明确回应，未反复重试或修改认证配置。
- 新增仅运行于临时 SQLite 的三处故障注入：原消费版本更新、退款关联审计写入、幂等响应记录写入。验证退款交易/分录/新账户/审计/幂等记录计数、完整汇总、原消费与账本版本全部回滚；移除故障后原 key/version 可以恢复并重复重放而不重账。
- 新增同 Store 下 8 个并发同 key 请求：全部返回同一退款，原消费版本和账本版本各只增 1，退款/分录/幂等记录不重复。独立数据库连接的并发上限继续由既有测试覆盖，不能把此次测试称为跨进程并发验收。
- 新测试 race 模式重复 5 次通过；随后全量 Go race/vet 通过。共享 JVM 测试、Web 生产和 Mac 分发构建再次通过（客户端和生产服务未改，构建任务复用有效缓存）。独立审查两项新增测试通过，无确认问题。

剩余门槛未变：Mac 真登录后完整退款验收、Web 角色/非法输入/丢响应实际操作及最新提交 CI。不得用自动化测试填补实际操作记录，PR 保持草稿。
