# M-09 本人账户间转账验证（2026-09-19）

状态：开发实现及本地自动检查通过；双端实际操作验收未完成。分支 `feat/internal-transfers`、[草稿 PR #14](https://github.com/letrahoo/monee/pull/14)，基于 M-08 主干 `72f2a37`。此文所在提交对应本轮实现；不能沿用前置纯函数提交的 CI 作为当前功能证据。

## 实现范围

- CNY 明确本人账户 A→B 的登记，不实际转款；独立 transfer 类型，正数为转账量，不是收入。两侧 clearing 分录相抵，不计收入、消费毛额、退款、净支出或消费分类；两侧账户均可搜索。
- schema4 升级前快照、固定连接事务重建/FK 检查/失败回滚，保留原交易、退款关联、来源、身份与审计。转账只允许修改备注，拒绝普通收支更正、消费退款和分类重写。
- 专用 POST /api/v1/transfers；逐请求授权、Cookie CSRF/Origin、账本隔离、规范化幂等、事务审计/版本。撤权或降为只读后不能重放成功请求。
- 共享登记表单需确认本人账户，明确双方、金额及日期；中性转账金额与方向展示。未确认结果由 API 按服务/用户/账本持有，失败/关闭表单/页面重建保留原请求和 key；不保证整页刷新/进程退出后的 pending 持久化。
- 不包括 M-10 还款、M-11 跨来源匹配、手续费拆分、跨币种和真实资产余额。原生未确认转账仍保留待核实，不自动匹配落账。

## 已完成的自动检查

- `go -C server test -race ./...` 与 `go -C server vet ./...` 全通过。
- Store：收支/退款合计不变、转账单独月份全零、两侧搜索、相抵分录、备注不改变分录、非法更正/退款拒绝、重复 key 与跨操作冲突、两个 Store 并发八请求仅落一笔、重开/快照恢复重放一致。
- 故障注入：第二笔资金分录、审计、账本版本及幂等落盘失败均原子回滚，恢复后重试唯一。
- 迁移：v3 退款到 v4 原记录/分录/审计不变；升级快照可恢复 v3；损坏 FK 导致回滚且恢复 FK enforcement。
- HTTP：匿名、只读、撤权、跨账本拒绝；editor 成功且降权后已成功 key 也拒绝；owner 跨账本同 key 不混淆；Cookie CSRF、恶意 Origin；非法精度/零/越界/账户/币种/未知字段。
- `:shared:jvmTest`、`:webApp:wasmJsBrowserDistribution`、`:desktopApp:createDistributable --offline` 通过（Temurin 17）。新增3项测试验证请求持有及隔离、账户限制、转账中性展示与旧 DTO 兼容。Web 保留既有 bundle size/dynamic dependency 警告，没有构建失败。
- 独立 Agent 对后端与共享客户端两轮只读审查：未发现可确认 P1/P2；后端相关专项 race 独立重跑通过。审查不替代实际验收。

## 实测待完成（不得据自动检查标为完成）

- [ ] Web 与 Mac 通过测试域名、各自正常 OAuth 会话，在隔离合成账本登记转账；双方明细及收支一致。
- [ ] 同账户、缺失侧、零、异常精度、越界以及原生转账待核实实际交互。
- [ ] 两端成功响应丢失、关闭/页面重建后原请求重试，仅一笔；两端只读、降权及撤权验证。
- [ ] 候选服务重启持久化、备注修改保护、既有退款回归。
- [ ] 当前最终 SHA GitHub 全 CI 及最终审查通过；回写 Notion 后才合并。

## 环境记录

原测试域名仍指向原4173/schema2服务，未写入真实账本。隔离候选卷保留。重启后 Colima 存在过期 VZ PID，已只读确认其指向无关进程，改名保留过期状态文件后成功启动现有虚拟机；未强杀进程、删除磁盘或影响其他站点认证。系统实际域名解析/可信 HTTPS 正常，先前受限诊断中的解析失败不作为 DNS 配置故障；未修改系统 DNS/代理。
