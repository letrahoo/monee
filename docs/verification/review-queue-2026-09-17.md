# 跨批次待核实清单验证（2026-09-17）

基线：main `38297e1`（PR #9 已合并；Linux / Mac ARM64 / Mac AMD64 CI 通过）。本轮分支 `feat/import-review-queue`，验证代码提交 `9895f72`，后续文档补记见对应 PR。

## 本轮范围

继续 M-04，提供只读的跨批次来源问题列表。Go 按账本成员权限查询已有 preview_json.pending，固定每页 50 条，支持支付宝/微信格式筛选；已关联正式交易的同批来源行排除。每项带稳定批次/行标识、来源账户、文件、原始金额/日期/商户、问题原因及该行原始字段。

疑问行不因批次未确认或撤销而丢失；批次状态独立标记为 preview/committed/undone/failed。纯文件级解析失败仍在导入历史里。不同文件重复出现的问题行各自作为来源证据展示，不声称已经去重成唯一交易。客户端不汇总这些原始金额、不四舍五入、不自动入账，原始字段不发送云端。

没有 schema 迁移或业务数据写入接口。确认退款、转账、忽略疑问和 AI 处理不是本轮范围，不把 M-04/M-29 或全部 P0 标为全部完成。

## 自动验证

- Go 全量 race 与 vet 通过。
- 定向测试覆盖未确认 all-pending、已入账排除、原始精度/编号、撤销恢复状态、失败批次边界、跨文件证据不合并、分页与格式筛选、来源账户区分/旧字段兼容、只读成员、移除成员、跨账本与未登录请求、查询前后数据库状态不变。
- 共享 JVM 测试、Web 生产构建、Mac 分发构建通过；原始 1.001 金额与前导零订单号契约测试通过。现有 Web 包体积警告保留。
- 只读复核发现 failed 也可能来自流水冲突，已将界面状态改为“批次处理失败”，避免错误提示文件解析失败。
- 初次 UI 编译发现颜色常量为文件私有，已将既有分隔线颜色共享后重验通过。
- 增量构建检查发现生成 Go 与 App 内置 Go build ID 不一致，打包任务错误地视为 UP-TO-DATE。已显式将 bundleDesktopResources 的目录声明为 createDistributable 输入。修复后增量打包实际执行，包内 Go build ID 与最新生成文件一致，全部 8 个 Web 文件 SHA-256/路径集合与生产产物一致，Go 程序保有执行权限，deep codesign 验证通过。

## 实际操作验证

Mac 补验已观察到打包 App 恢复有效登录、进入隔离账本（3 笔、支出 12.34、收入 115），打开待核实清单显示 5 条来源记录、来源账户、批次状态及原始 1.001 金额。随后点击支付宝筛选时，界面工具再次报告 Mac 锁定，无法继续操作；筛选、展开原文、批次跳转和异常/权限组合仍未验收。上一轮遗漏的 Mac 更正/异常流程也仍待补。打包 App 使用独立测试目录并自动拉起新版服务，Web 使用同一随包资源。

Web 已用既有有效登录在隔离合成账本完成：

1. 进入待核实清单显示 5 条来源记录；支付宝筛选 2 条、微信筛选 3 条，与两批次保留记录一致。
2. 原始金额 1.001 未被取整；展开显示原始分类、收支、流水号、商家订单号及支付状态。
3. 查看导入批次回到相应文件、行号与两条疑问说明，返回清单可继续筛选。
4. 浏览与筛选前后正式账本均为 3 笔、支出 12.34、收入 115，没有将疑问计入汇总。
5. 停止明确属于验证的 App/服务后，Web 显示连接失败；恢复打包 App/服务后，原有有效会话恢复到工作台，再次进入合成账本能读到同样 5 条记录，无再次 OAuth。

Web 此轮未实测分页、空来源、多账户/未确认/撤销批次的完整界面组合或第二账号只读/撤权；这些规则由自动化测试覆盖，不能记作 UI 全部通过。没有向正式账本写入合成数据；本轮未变更隔离账本的财务记录。

## 后续验收

完整只读/撤权/网络异常的双端界面、更多真实来源格式仍需验收；合成数据测试不能替代真实账单格式验证。本轮无云端模型调用。
