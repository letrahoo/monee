# 原生账单导入与桌面登录缓存验证

日期：2026-09-17。基础提交 `fe24fd8`；被测内容为 `feat/alipay-import-preview` 本次功能提交，提交号以 PR 中的验证说明为准。环境为 macOS、JDK 17、Go 本地同源服务、Compose Web 生产资源和 Mac 分发构建。独立数据目录与合成账本；仅通过真实登录访问，未绕过权限，没有调用收费模型。

## 自动检查

- Go 全量 `test -race ./...`、`vet ./...`、服务端构建通过。
- `:shared:jvmTest`、`:webApp:wasmJsBrowserDistribution`、`:desktopApp:createDistributable` 通过。Web 现有包体积/动态依赖警告仍存在。
- OpenAPI YAML 解析与 Git 空白检查通过。
- 支付宝编码、稳定来源标识、重复/冲突和异常记录隔离；微信 XLSX 原样精度、公式/数值订单号/多工作表/超限/畸形结构拒绝均有合成测试。
- 原生失败批次留存、重复确认幂等、来源关联和修订历史、乐观锁冲突、只读成员写入拒绝、跨账本隔离有后端测试。
- `MONEE_TEST_KEYCHAIN=1` 的桌面合成凭据测试实际执行，通过保存、读取、删除；普通单测另覆盖凭据输入和环境隔离。

## 实际界面结果

| 流程 | Web | Mac |
| --- | --- | --- |
| 合成微信 XLSX | 选文件 → 1 笔普通支出 + 3 笔待核实 → 确认通过 | 已从账本查看结果；未重新执行文件选择 |
| 合成支付宝 CSV | 已查看 Mac 导入结果 | 选文件 → 2 笔普通收支 + 2 笔待核实 → 确认通过 |
| 跨端统计 | 3 笔，支出 24.68、收入 100.00 | 与 Web 一致 |
| 重复微信文件 | 重新选择同文件并预览，提示已入账；仍为 3 笔，修订分类保持 | 后端自动测试覆盖，未再次通过 Mac 上传 |
| 分类/备注 | 保存后分类统计变化、金额不变，修改记录可见 | 刷新后可见 Web 的修订；未单独提交编辑 |
| 来源追溯 | 交易 → 批次 → 原文件第 2 行，可见未修改原文 | 同一路径通过，原始备注与修订后的备注分别保留 |
| 会话恢复 | 浏览器已有会话继续有效 | 用户完成 GitHub 登录 → 重启进程 → 自动进入已登录界面 |
| 主动退出 | 本次未操作 | 退出 → 登录首页 → 重启仍为登录首页 |

合成资料：`fixtures/synthetic/wechat/ordinary-and-review.xlsx`。支付宝界面测试资料仅在本地验证目录；后端测试包含可复现的内嵌合成样本。没有把真实账号画面、数据库、原账单或凭据提交到 Git。

## 未完成的验收与产品范围

畸形文件的两端错误页、只读角色的全部界面、Mac 独立编辑/重复文件操作、窄屏和键盘导航仍未逐项实机完成；当前不能宣称全部 UI 回归完成。自动测试不代替这些人工用例。

真实用户导出格式尚需样本验收；微信暂只接受单工作表 XLSX 和已知表头，不支持 XLS、数值订单号或公式单元格。金额/类型修订、撤销、退款/转账/还款、跨来源关联、AI 脱敏、更多银行及京东导入、Mac 服务托管不属于本次已实现范围。Notion P0 仍有后续任务。
