# PR #6 验证记录

日期：2026-09-15。功能提交：734f810。结论：自动化与构建通过，两端启动冒烟通过，登录后端到端验收尚未完成。

## 已通过

| 层级 | 用例与实际结果 |
| --- | --- |
| Go | 全量 race 测试、vet、服务构建通过；覆盖解析边界、旧迁移兼容、重复导入、来源追溯、幂等及账本权限 |
| 共享客户端 | 本地 JVM 测试通过 |
| Web | 本地 Wasm 生产构建通过；Chrome 实际加载同源 Go 服务页面，登录首页与品牌正确显示，隐私说明可展开 |
| Mac | 本地 createDistributable 通过；启动新构建的应用并明确指向独立空账本，登录首页显示正确，隐私说明可展开 |
| 未配置登录 | 两端提示登录暂不可用，不呈现账本；新增历史和详情 API 的未登录请求均返回 401；健康接口返回 200 |
| CI | [功能提交构建](https://github.com/letrahoo/monee/actions/runs/34939217765)：Linux、Mac Apple Silicon、Mac Intel 三项均通过；PR 不执行发布 |

环境：macOS Apple Silicon、JDK 17、仓库锁定 Gradle/Kotlin/Go 版本。独立验证服务使用回环端口 4273 和新空数据库，没有访问正式账本，没有调用云端模型。

Web 构建有动态依赖与包体积警告，未导致失败；未进行性能专项验收。浏览器连接接口初始化失败后，通过 Chrome 原生无障碍界面完成上述操作；不将工具尝试本身记为通过。

## 待验收

初次补验缺少隔离 OAuth 环境。后续经用户要求已停止旧服务及 4273 测试服务，在 4173 启动当前提交的后端，复用本地私有 OAuth 配置并使用独立空数据库。Go 检查、共享测试和两端构建再次通过；提交 eab4508 的三平台 CI 也已通过。

Mac 已从 GitHub 登录按钮完成真实授权并进入空账本首页；Web 也已观察到登录后的空账本首页。未修改正式数据或绕过鉴权。完整导入验证仍未完成：浏览器后台控制接口初始化失败，原生窗口操作持续受到前台切换和剪贴板干扰；新开独立窗口仍未解决控制冲突，未将这些尝试记为成功。

以下用例须取得可靠界面操作环境后继续验收：

- Web 和 Mac 各自登录、退出、重新登录；Mac 浏览器回调返回应用。
- 两端导入合成标准 CSV，核对预览、提交、交易列表与统计；重复文件和换行不同文件不重复记账。
- 两端异常文件、权限拒绝、刷新/重新进入后的状态一致性。
- 通过真实登录会话核对新增批次列表与详情。当前客户端没有历史页面入口，不能声称该界面已验收或已交付；API 已有隔离自动化用例。

完成后记录对应提交和逐项实际结果，不以构建或接口测试替代两端操作验证。PR 保持未合并，产品任务保持进行中。
