# 开发交接

更新：2026-09-19。本文是换电脑、换会话或换开发者的入口。当前主干基线为 `9c04177`；后续提交见 Git 历史，当前未提交工作见下方接续章节。真实凭据、身份配置和账本不在仓库内。

## 最新接续：本人账户间转账 M-09（2026-09-19）

2026-09-20 标准启动与修复（优先于下方“尚待批准”）：用户要求继续修复后，已使用 Launch Services 保留既有 HTTPS/CA/客户端作用域启动。两次线程快照（启动约 81 秒、281 秒）均显示 AWT 事件线程在首次构造 API 时等待 CA 文件读取，界面无法响应；是否为系统文件访问提示尚未确认，不将其当作原自动输入问题的根因。已将桌面配置预热移至后台 IO，UI 在完成前只显示准备状态，取消不创建客户端，配置失败沿用原失败缓存与 TLS 拒绝路径，不回退本地或放宽认证。新增阻塞读取期间 EDT 响应与取消门控两项测试，Go race/vet、共享 JVM 测试、Web 生产及 Mac 分发构建通过；独立审查未发现 P1/P2。新包标准启动已恢复原登录，实际进入纯合成 Mac QA 账本（0 笔、收支全零），按钮操作正常；自动文字及激活窗口后逐键输入仍未改变金额，已请用户只手动输入合成金额作对照、暂不保存。真实阻塞期间加载页交互尚未实际复现，不能用单元测试替代。新增修复尚待对应提交 CI，Mac 转账及其余门槛未齐，PR #14 不合并。

2026-09-20 权限与边界补验：经用户针对 `M09 Web Pure QA 0920` 的明确批准，Web 所有者邀请既有 Google 测试身份只读加入，Mac 正常接受。Mac 看见同一笔 `100.01`、相同方向/备注及全零收支，转账入口禁用、备注编辑入口隐藏；升为 editor 后刷新恢复入口，打开空转账表单后降回 viewer，刷新关闭表单并禁用写入；移除成员后刷新回到账本列表，原详情与目标账本均消失。Web 成员管理确认只剩所有者，已清理新增权限。仅完成 Mac 受限视图/角色刷新/撤权界面验收，未完成 editor 实际写入或降权后提交、Web 受限会话及幂等权限重放，不标记完整双端权限通过。Web 另实测 `2026-02-30` 被服务端拒绝，越界 `92233720368547758.08` 在归属已确认时禁用保存，合法 `1.00` 恢复可用但未提交，账本仍 1 笔、收支全零。Go race/vet、共享测试、Web 构建再次通过，74bac5b 六项 GitHub CI 成功。

Mac 输入新对照：原生 Monee 菜单可用 AX 点击打开，但自动 Escape 不能关闭，AX Cancel 可以关闭；不能只归因 Compose 文本框。独立只读审查建议正常退出后，用 macOS Launch Services 的 `open --env` 保留原 HTTPS/CA/客户端数据作用域启动同一包，对照原生菜单 Escape 与纯合成备注输入；已向用户请求这一启动方式对照，尚待批准，未执行、未改权限或源码。下一步优先接续该对照；PR #14 未合并，不领取 M-10。

2026-09-20 Web 备注补验：在独立纯合成 QA 账本将唯一转账备注从 `M09-WEB-01` 改为 `M09-WEB-01-NOTE-EDIT`，收到保存成功，修改记录显示旧/新备注及本人转账分类未变；完整页面重载、重新进入账本后新备注仍在，金额 `100.01`、账户方向、1 笔及全部收支统计 0 不变。详情只提供修改备注，没有普通金额/分类/退款入口。此为 Web 备注与页面重载验收，不代替 Mac、权限、丢响应或服务重启门槛；PR #14 仍不合并。

同轮 Web 补验通过：页面重载后恢复正常会话，重新进入 QA 账本仍 1 笔且收支全零；归属已确认时，零金额、三位小数、同账户、缺失转入账户均不可保存，合法 `1.00` 正对照可保存但未提交。已退出边界用例表单，保留 Web QA 页面供继续。仍需权限、丢响应、服务重启与 Mac 等门槛，不能据此合并。

2026-09-20 Web 恢复与首笔转账实测（优先于以下 Web 策略阻塞记录）：已读最新 `PROJECT_CONTEXT.md`，原型能力不等于正式应用。通过正规浏览器接口及既有 GitHub OAuth 恢复 Web 会话，创建独立 `M09 Web Pure QA 0920`，实际保存合成 `100.01`、`QA-Web-A` → `QA-Web-B`、备注 `M09-WEB-01`。页面显示“转账已保存，收支不变”，1 笔，收入/消费毛额/退款/净支出全部 0；详情方向、日期及备注一致。转入账户搜索从零结果恢复为 1 笔，不匹配账户返回零且统计不变。Mac 输入问题及双端权限/异常/丢响应/服务重启验收仍待完成，未合并 PR #14；74bac5b 六项 CI 仍成功。当前 Web 账本属 GitHub 会话，与 Mac 会话的纯合成账本独立，未改成员权限。

2026-09-20 输入对照与启动恢复（优先于下方等待填表记录）：本次重新连接 App 时显示“本地服务地址无效”，正常关闭后使用既有 HTTPS 测试启动脚本及原隔离客户端作用域重启，恢复原登录和三个账本；可信 health 核对仍为原 schema4 候选，未改配置、路由或服务器数据。进入 `M09 Pure QA 0920` 确认仍 0 笔。备注字段自动文字无效，剪贴板等待应用读取超时，原生窗口/字段坐标点击后仍无效，Tab 导航也未见焦点移动。新增对照：同一工具可向 macOS 系统设置搜索框正常输入 `Accessibility`，测试后已清空；权限页显示 Codex Computer Use 开启，全程未切换权限。证据不支持笼统归因全局输入权限缺失，但仍不足以区分 Monee 原生键盘焦点与自动化/Compose 兼容问题。尚未提交转账，不合并 PR。不要自动开启其他驱动权限或绕过 Web 策略。

2026-09-20 用户交接后的复核（优先于下方未创建记录）：已在 Mac 正常会话进入用户新建的 `M09 Pure QA 0920`，界面确认交易 0 笔、当月收支及退款全零，并打开本人转账表单。自动文字及逐键输入仍未改变金额字段，未提交转账；已请用户填写合成金额 `100.01`、账户 `QA-A` → `QA-B`、备注 `M09-MAC-01`，先不保存，待交接后再核对并提交。当前等待填表回应，不得把空账本准备算作转账验收。Dock 图标实际确认及 Web 策略恢复仍待完成。

2026-09-20 桌面重打包接续（优先于以下等待退出记录）：用户确认已退出，进程核对无 Monee 后完成共享 JVM 测试、Web 生产构建及 Mac createDistributable，打包/签名校验通过。已通过原测试启动脚本启动包含 DesktopBrand 且移除临时输入监听的新包，沿用既有 HTTPS origin/CA 和客户端数据作用域；正常恢复登录并显示原两个账本，测试实例 health 仍为既有 schema4 候选，未改服务器/路由/数据。Dock 原生观察超时，等待用户目视确认图标；不能仅凭打包成功宣布图标验收通过。新建纯合成账本字段的自动 typeText 仍未生效，后续粘贴提示用户正在操作，重新读取后字段仍空、创建按钮禁用；已停止界面操作避免干扰，请用户创建 `M09 Pure QA 0920` 并通知可接手。没有新建成功或转账提交证据，Web 既有安全策略阻塞未确认恢复，PR 仍不合并。

最新输入恢复复核（优先于以下历史阻塞）：用户已确认手动输入恢复；本次 Mac 界面读取也可见用户输入的字段值，不能再称手动输入持续失败。原生自动化能读界面和点击，但新建纯合成账本时 `typeText` 未改变字段、粘贴等待超时、AX setValue 不受支持，因此未创建新账本或提交测试转账。一次 noWindowsAvailable 后重新绑定恢复，进程仍在运行，不能据此判断 App 已退出。Web 正规接口再次返回管理员策略无法核验，未换路径绕过。测试域名 health 确认仍为既有 schema4 候选，未改路由或数据。已删除本工作区的临时输入事件监听代码；共享测试、桌面编译、Web 构建、Go race/vet 再次通过。图标补丁仍待用户正常退出后重打 Mac 分发包和目视验收。Notion 仅更新手动输入恢复、自动化阻塞及构建状态，已确认写入成功；未披露先前被拒绝的数据处理信息。PR #14 的 74bac5b 六项 CI 全通过，仍为草稿，验收未齐不合并。

最新桌面图标排查：用户确认测试启动后 Dock/程序切换器图标错误，窗口已出现。包内 ICNS 与生成产物哈希一致且目视为正确品牌，运行时尚未显式设置 Taskbar 图标。工作区新增 `DesktopBrand`，从共享包读取既有品牌 PNG，在 AWT 事件线程显式设置受支持的 Taskbar 图标；Main 的窗口初始化调用它，保留原输入诊断改动。新增资源哈希/透明度测试，共享 JVM 测试、桌面编译、Web 构建和全量 Go race/vet 通过。尚未重打 Mac 分发包：正在等待用户正常退出当前 App，避免覆盖运行中的包；新包 Dock/程序切换器仍待实际确认。未宣称根因或输入问题已经修复，不因此合并 PR。

最新数据边界（优先于下方“合成账本”描述）：用户明确要求把历史真实交易副本导入当前测试账本供本人查看，已通过正常 Mac 登录会话、标准 CSV 预览和确认完成，导入前成员管理确认只有所有者。该预览账本及其所在候选数据卷现在含真实私有数据，**不得再按纯合成资料共享、截图提交、邀请测试成员、故障注入、重置或删除**。后续 M-09 功能验收必须改用新的纯合成账本；实例级破坏性测试必须另建不含私有数据的实例。私有回执留在本机忽略目录，真实路径、数量、金额、流水和快照不进入 Git/Notion。现有测试域名路由保留供用户查看；路由恢复前应先说明预览数据所在实例，不能令用户误以为导入丢失。此导入不是 M-09 转账验收，不解决输入异常或 Web 工具策略阻塞，PR 不因此合并。

M-08 已通过双端隔离合成账本全部必需验收、独立 Agent/GitHub 审查，最终提交 `6b79b3b` 六项 CI 全成功；PR #13 已合并主干 `72f2a37`，Notion M-08 已完成。证据见[退款验证](verification/refund-linking-2026-09-19.md)，不是生产部署或整个批次 MVP 完成。

已从该主干领取 Notion M-09，分支 `feat/internal-transfers`、草稿 PR #14。[实施计划](plans/internal-transfers.md)限定明确双方的 CNY 本人资金转移、不计收支、缺失侧待核实。现已实现 schema4 安全迁移/正式双资金分录、统计排除、两侧搜索、编辑保护、权限/幂等 API 及共享 UI。全量 Go race/vet、共享测试、Web/Mac 构建通过；独立后端与共享客户端审查无 P1/P2。当前仍未完成双端真实转账验收，不合并、不领取M-10。见[验证与剩余项](verification/internal-transfers-2026-09-19.md)。

最新临时验收状态（本段优先于旧环境记录）：`d6d9b2e` 已推送 PR #14，GitHub Codex 审查无重大问题。当前测试域名两处路由**临时指向隔离4280/schema4候选**，原4173/schema2仍运行且原账本未写入。Web/Mac正常会话读到候选，新建 `M09 Transfer Synthetic 0919` 合成账本但未提交转账。Web工具管理员策略核验失败，禁止间接绕过，已请求用户重新检查页面；Mac转账/普通记账均无法输入，用户手动也报告相同症状，重启后仍复现。

正在排查Mac输入，不得写成工具问题已解决：Main.kt 有**未提交的临时** `MONEE_INPUT_DIAGNOSTICS=1` 诊断代码，只记录鼠标/按键事件种类和窗口焦点，不记录键码/字符/剪贴板。诊断包开在M09合成账本表单，事件显示鼠标到达但 `active=false focused=false owner=null`，尚无键盘事件；不能据此断定用户实际手动输入的根因。下一步暂停自动操作，请用户直接在金额框键入合成123（不要保存），读取诊断区分原生焦点与表单；定位后删除诊断代码并重新完整验证。原部署配置备份及启动信息在本机忽略目录/临时目录，不提交。临时验收结束或无法继续时应恢复两处4173路由，保留Bearer修复和外层密码取消。

后续只读复核：`74bac5b` 的六项 GitHub CI 已全部成功（release 按 PR 条件跳过）。Transfer/Schema4 相关 ledger/httpapi race 回归重复五次通过。现有诊断进程随后输出了 `KeyEvent` 401/402、`active=true focused=true owner=SkiaLayer$1`；说明此前“没有键盘事件”不是持续状态，但未观察到表单文本成功改变，不能宣布输入修复。当前 Mac 自动化绑定连续超时，未强杀应用、未触碰账本或路由，也未绕过 Web 策略；仍需新的纯合成账本实际验收。此前 Notion 数据处理状态回写被审批拒绝，尚未获得用户对该状态披露的确认，不重试该写入。

独立输入链路只读审查未发现可由代码确认的 P1/P2：监听器不消费事件、登录回跳不持续抢焦点、普通字段更新未被定时刷新重置。诊断缺口是 401/402 仅代表按下/释放，且窗口焦点延迟采样，不能证明字符进入 Compose。待 UI 可观察后，仅在新纯合成账本的非金额字段测试 ASCII，核对 KEY_TYPED/输入法提交、字段焦点、onValueChange 次数与 working/pending 布尔状态，不记录字符/账号/账单。没有证据时不改依赖、启动机制或认证链路。

Colima过期VZ PID已改名保留后恢复原VM，未强杀/删卷。此前批准的精确域名代理例外重启后缺失，本次已恢复且保留其他例外/代理。JDK位于忽略的`.local/toolchains/`，Gradle用`.local/gradle`。以下M-08历史等待/阻塞以本节为准。

## 最新接续：Mac 测试域名连接（2026-09-19）

最新收尾（优先于本节以下历史记录）：Mac 更正保护与主动退出/重启退出态已实际通过，双端必需验收齐备。`2d691a9` 六项 CI 成功，GitHub Codex 审查无重大问题。当前仅补证据文档，需等最新文档提交 CI 后按授权合并 PR #13，再领取 M-09；未合并前不标记完成。Mac 隔离客户端已退出，新增测试成员此前已移除；两处 Monee 路由已恢复 `127.0.0.1:4173`/schema 2，LAN/Tailnet 实例一致，匿名及无效 Bearer 401、HTTP 308、resume/Hermes 401。保留网关 Bearer 修复、外层密码取消、原数据及候选合成卷，不部署生产。

最新实测已推进：用户批准代理例外后浏览器可操作，Mac `9d5e63f` 分发包已通过测试域名真实 Google OAuth、桌面 proof 领取、Bearer 读写；隔离账本 `M08 Mac Synthetic 0919` 部分/累计全额/跨月退款、超额拒绝、零与精度输入限制、进程重启恢复会话和数据通过。Web 独立登录核对一致。两端“成功响应丢失”均已实际复现；Mac关闭面板重试、Web切换账本列表重建页面再重试均无重复记账。临时故障注入规则已移除。双端只读/撤权和 Mac 撤销/恢复关联保护已通过，获用户明确批准新增的合成账本测试成员已移除。当前仍需 Mac 更正金额保护输入和退出态验证；深层表单输入遇到工具剪贴板超时，已请求用户把App切至前台。继续 M-08，不领取 M-09、不合并。此段优先于以下旧阻塞记录，详细证据见 [退款验证](verification/refund-linking-2026-09-19.md)。

用户最新要求优先跑通 Mac App 直连测试域名，再诊断浏览器策略。此节优先于下方“等待新增 4278 回调”的旧记录：不再以新增本机回调为当前方案，不擅自修改 OAuth 提供方设置。

已新增显式 HTTPS 模式与测试启动脚本，默认本地模式保留。Java HTTP 引擎使用独立 CA 信任与主机名校验、禁止重定向，仅明确配置的测试 origin 不走系统 Web 代理。完整共享测试（含真实域名只读 health、真实 TLS 异常用例）、Web/Mac 构建通过；打包 App 实际显示 Google/GitHub 登录入口。尚未完成登录领取和账本实际操作，不能称完整链路已通。

网关阻塞已解除：用户随后明确批准修复，已备份维护配置、仅过滤 Monee 遗留 Basic 并保留 Bearer，校验和 reload 成功。HTTPS/Tailnet health 200，账本接口匿名及无效 Bearer 401，其他站点仍 401。没有绕过审批；其他站点认证和 Monee OAuth/准入/账本权限不变。域名仍指向原 schema 2 服务，不能试写真实账本。有效登录后的 Bearer 完整链路仍待验收。

浏览器正规接口再次返回管理员策略无法核验，访问未获授权；没有用其他浏览器/原生浏览器操作绕过。后续需用户核对 Codex 的站点授权/工作区策略，不能把 Monee 密钥或 OAuth 回调当作该策略错误的修复。详细测试与未完成项见 [域名连接验证](verification/desktop-test-domain-2026-09-19.md)。M-08 和 PR #13 保持待验收/草稿。

## 当前接续：全额与部分退款 M-08（2026-09-19）

PR #12 已按用户授权合入 main `9c04177`。最终功能提交 `190b44c` 六项 CI 全部通过，GitHub Agent 最终复查无重大问题，审查讨论均已修复并解决。候选 Web 已完成真实 Google/GitHub 登录、独立合成账本写入、API 替换/刷新持久化、退出及身份隔离验收；最新 Mac ARM64 CI 包已实际验证独立本机服务连接及登录入口，不宣称 Mac 远程 HTTPS 或重复了完整真实 OAuth/记账流程。主干项目构建 `35380826678`、首次镜像发布 `35380826645` 均成功；API/Web digest artifact 的 commit/run 已核对，并通过 GHCR manifest 实际读取确认，Notion M-38 已结项。未自动部署生产。

用户明确取消 Monee 外层 Basic Auth。已仅移除本机 Monee LAN/Tailnet 两入口的外层密码，保留应用 OAuth/准入/账本权限、安全头和其他站点认证；原服务上游已恢复，真实账本未试写。后续恢复测试路由时不能重新启用用户已取消的认证层。

当前 `feat/refund-linking` 已实现 schema 3 迁移/备份/回滚、退款登记与查询、额度/幂等/并发/版本、月度及分类毛额退款净额、更正/分类/导入撤销关联保护与共享登记/明细 UI。已开放专用退款 API，但双端实际退款流程尚未验收，不能合并或标记已交付。[实施计划](plans/refund-linking.md)限定：退款独立关联消费、退款发生日抵减该期支出；原生来源自动匹配仍属后续任务。

用户要求 Loop 持续到增量 MVP：当前任务已启用 `monee-mvp` 自动接续（15 分钟）。本轮以 Notion 第二批「账目能算对」为里程碑：M-08 → M-09 → M-10 → M-11，并补相关 M-05/06/07 的双端验收缺口；不等于全部第一期来源/AI/自动化已完成，不扩展第二期。每项完整开发、测试/双端实测、独立审查和 GitHub CI 通过后，按已有授权直接合并、回写 Notion并领取下一项。仅有构建成功不允许合并，阻塞须保留证据，不得绕过鉴权或写真实账本。

对应[草稿 PR #13](https://github.com/letrahoo/monee/pull/13)。功能提交 `108116c` 的六项 GitHub CI、Go 全量 race/vet、共享测试和 Web/Mac 构建通过。独立审查发现 UI pending 不能只放页面，已提升到 App 持有的 API，按服务/用户/账本隔离，复查确认解决；此保证不包含整页重载或进程退出。

最新实际验收：Chrome 真实 GitHub 登录、独立合成账本的 100/30/70 全部分次退款、超额拒绝、跨月与仅退款月负净额、API 重启持久化、更正保护、撤销/恢复的退款依赖保护均通过。Mac 同提交分发包连接独立回环服务成功，但 GitHub/Google 均拒绝端口 4278 回调，登录后验收未完成。已请求用户允许现有 GitHub App 新增 `http://127.0.0.1:4278/auth/callback/github`，尚未获得回应；不要自行改认证配置或反复重试同一无效回调。Web 仍需补角色交互、更多非法输入、丢响应恢复实测，Mac 仍需完整退款流程。先补这些同任务验收，满足门槛后再合并和领取下一项。

临时 Monee 路由已恢复原 4173/schema 2 服务，Monee 外层密码保持取消，其他站点认证未变；独立 Mac 测试进程已停止，候选容器/合成数据保留。退款 schema 3 仅在测试候选生效，不得在旧 UI/原服务/真实账本试写。[当前验证及剩余项](verification/refund-linking-2026-09-19.md)。

最新续验补充：`246bf24` 六项 CI 全通过。浏览器工具出现“管理员强制策略无法验证”拒绝，未绕过或改用间接 UI 控制，未新增 Web 实测通过项；临时路由已立即恢复。新增临时库后半段失败原子回滚与 8 次同 key 并发重试测试，race 重复 5 次、全量 Go race/vet、共享/Web/Mac 自动检查通过，独立审查通过。仍需等待浏览器策略可正常验证、Mac 回调获确认后再完成双端剩余验收；外部状态未变时不要反复发起同一失败操作或重复催问。

## 历史接续：HTTPS 容器化生产部署（2026-09-18）

当前分支 `feat/compose-deployment` 基于 main `f0b06a4`，对应 Notion M-38、PR #12，尚未合并或上线。

- 新增 Go API / Kotlin-Wasm Web 双镜像、Compose 编排、GHCR 构建发布工作流、非 root/只读运行约束、健康检查、同源反向代理和固定镜像 digest 回滚流程；SHA 标签不是不可变部署引用。
- Go 服务新增显式 HTTPS 公网 Origin；生产 Cookie 使用 Secure，Host/Origin 防护继续生效。本地回环 HTTP 保持兼容。
- API 镜像携带一致性数据库快照/离线恢复工具；OAuth 配置、`redaction.key` 与数据库快照仍须分别加密备份。
- Go race/vet、共享 JVM 测试、Web 生产构建、Mac 分发构建及本机生产 Origin/Cookie 合成验证通过。
- PR #12 提交 `fee2d61` 的三平台项目 CI、API/Web 镜像和完整 Compose 联调六项检查均通过。已补 OAuth 出网、API 更换 IP 自动恢复、查询参数日志保护、日志轮转、digest 发布记录及 DB/WAL/SHM 整组回滚。GitHub 后续复查的 IPv6/IDN 问题继续修复：规范 IPv6，IDN 要求 ASCII punycode，拒绝歧义地址。
- 用户已授权临时测试路由、独立/GitHub Agent 复查及全部门槛通过后直接合并，不需要重新请求这些授权。已在独立候选卷验证真实测试域名的可信 HTTPS、页面/刷新路由/资源和 401 边界，原账本未写入。浏览器仍需完成外层 Basic Auth，真实 OAuth 及登录后的独立账本操作尚未验收，不能合并或标记完成。接续须先核对临时路由是否已恢复；本机 ARM64 验收镜像与 CI 生产 AMD64 镜像的来源差异见 [M-38 验证](verification/compose-deployment-2026-09-18.md)。

## 当前接续：微信 CSV 与脱敏预览（2026-09-17）

本轮 `feat/wechat-csv-redaction-preview` 并行推进 M-25/M-26，功能提交 `690b04a`。PR #10 已由用户合并 main `e64f021`、三平台 CI 通过；本分支已在 `e431060` 整合最新主干，保留待核实清单和 Mac 增量打包修复。

- 微信同一入口支持 UTF-8/BOM、已知表头的 CSV 和既有单表 XLSX，按内容识别；保留物理行、原文、金额精度与订单号。复用普通商户支出校验和疑问分流，同账户同支付流水跨格式去重，旧 XLSX 批次兼容。真实 CSV 样本仍待补，不宣称覆盖全部微信导出格式。
- 原生导入批次可查看本地脱敏预览：保留规范日期、整数分金额和明确枚举；账户/商户/订单号替换为 HMAC 引用，商品/备注等原文不输出，无效记录单列原因。投影来自原始来源快照，不含后续账单修改。没有模型调用、发送操作或发送授权。
- 私有数据目录新增 `redaction.key`，安全迁移时须与账本一起备份恢复；损坏/权限异常时仅关闭脱敏能力。不能提交 Git；没有自动轮换界面。
- 双端实际验证与剩余边界见 [本轮验证](verification/wechat-csv-redaction-2026-09-17.md)。Notion M-25/M-26 分开跟踪，不把本轮当作全部 P0 完成。

## 历史接续：待核实清单（2026-09-17）

PR #9 已合入 main（`38297e1`），Linux、Mac ARM64/AMD64 CI 均通过。原 M-06/M-26 与 M-07/M-37 代码已进入主干；保留原验证记录中的 UI 验收缺口。

从该 main 基线创建 `feat/import-review-queue`，继续 M-04 来源追溯：新增账本内跨批次待核实清单、支付宝/微信筛选、来源账户区分、原始字段展开与批次返回。只列已保留原生批次的 pending，不把已入账交易、文件级解析错误或源记录条数当成待处理交易总额。

未确认、解析失败但有可用疑问行、已确认及已撤销批次均保留来源问题；按文件/行分别展示，不擅自跨文件合并。原始金额/日期可能无效，不能做四舍五入或统计。退款/转账处理、待核实项确认/忽略状态、AI 自动归类仍待后续任务；本轮无模型调用。

本轮验证和界面阻塞见 [待核实清单验证](verification/review-queue-2026-09-17.md)。

## 历史接续：撤销导入与桌面服务（2026-09-17）

本节优先于下方历史快照。PR #7 已合入 main（`5940941`）；PR #8 合入的是 `feat/alipay-import-preview`，其 M-06/M-26 当时未进入 main。本轮从 main 创建 `feat/import-undo-local-service`，合入 #8 对应提交（集成点 `5424589`），后续 PR 直接以 main 为目标。不能把“PR 已合并”当成“代码已进入 main”。

- M-07：按批次预览撤销/恢复，只撤销本批新增、未修改且未被其他批次关联的交易；修改或共享记录保留并展示原因。保留原始来源与分录，以软删除和版本日志保证可追溯、可恢复、重复请求不重复变化。
- M-37：Mac 分发包包含 Go 服务和 Web 产物；启动时核对 discovery/health 进程身份，复用匹配服务或启动内置服务，拒绝占用端口的不匹配服务。关闭 App 不关闭共享服务。
- Notion M-02/M-03/M-05/M-06 已按用户要求标记开发完成；M-06 Mac 验收缺口单列。M-26 仍只是程序脱敏模块，尚无密钥生命周期、发送预览和云端模型调用。
- 本轮完整验证与缺口见 [M-07/M-37 验证记录](verification/import-undo-service-2026-09-17.md)。退款、转账、还款、更多银行原生格式与 AI 自动处理等 P0 尚未全部完成。

## 历史增量（2026-09-17，后随 PR #7 合并）

本节优先于下方历史快照。开发分支 `feat/alipay-import-preview` 基于已合并的 PR #6（`fe24fd8`）；接续时先检查远程 PR 和未提交状态，不覆盖工作区。Notion P0 共 22 项，本次不是全量交付。

- 新增支付宝原生 CSV（UTF-8/GB18030）和微信单工作表 XLSX 预览、确认、导入历史；退款/转账/未知类型和异常精度保留待核实，不冒充普通收支。
- 来源文件内容、原始行与正式账单关联；可从交易返回批次和原文。原生解析失败也保留批次；相同原文件重试不会重复入账。
- 分类/备注修订有历史和版本冲突保护，源文件不被覆盖，重新导入不会抹掉修订。
- 调整登录、账本顶部和导入选项交互；Mac 新增钥匙串会话缓存。相同数据目录/服务地址且会话有效时，重构建后无需再授权；当前有效期仍为 12 小时。
- 后端 race/vet、共享测试、Web/Mac 构建和主要双端合成流程通过，具体通过项和未验收项见 [验证记录](verification/imports-and-session-2026-09-17.md)。不能把 P0 全部标成完成。
- 仍待实现：金额/类型修订、批次撤销、退款/转账/还款闭环、跨来源核对、AI 脱敏/自动处理、更多原生银行/京东格式和 Mac 管理服务。未进行云端模型调用。真实银行格式须用脱敏样本验收。

## 先读与先取什么

1. 读根目录 [AGENTS.md](../AGENTS.md)：实现边界与开发规则。
2. 按[异地开发指南](operations/development.md)取得当前分支、运行测试与应用。
3. 当前实现查[数据链路](architecture/local-data-flow.md)、[登录与权限](architecture/login-and-access.md)和 [OpenAPI](../api/openapi.yaml)。
4. [产品讨论](product/mvp-discussion.md)和[架构提案](architecture/mvp-proposal.md)包含后续愿景，不能据此声称功能已交付。

## 远程进度

2026-09-14 查询结果；接续前重新检查 PR 状态，不依赖这张历史快照自动合并。

| PR | 分支 → 基础分支 | 状态与内容 |
| --- | --- | --- |
| [#1](https://github.com/letrahoo/monee/pull/1) | feat/kmp-foundation → main | 已合并；KMP 基础与品牌图标 |
| [#2](https://github.com/letrahoo/monee/pull/2) | feat/local-ledger-api → main | 已合并；真实账本链路 |
| [#3](https://github.com/letrahoo/monee/pull/3) | feat/oauth-allowlist → feat/local-ledger-api | 已合并到原基础分支，其完整内容已补合入 main；登录、白名单、桌面返回、退出及文案精简 |

**继续开发直接检出 `main`。** #2 先合入 main，#3 随后合入 feat/local-ledger-api，曾导致 main 未包含后续认证代码；2026-09-14 已将完整开发分支补合到 main。功能分支仅保留历史，不再是接续入口。

重要提交：`277739f` 登录/白名单；`f4c4fea` 桌面自动返回；`916e715` 退出入口和状态清理；`3fde5bf` 全局文案精简。未提交内容和缓存不是交接依赖。

## 已定方案与取舍

- 先给产品负责人个人使用，Web / Mac 优先，iOS / Android 后续补齐。
- KMP + Compose Multiplatform 已确定；Web 为 Kotlin/Wasm，Mac 为 JVM/Compose Desktop，不是 WebView 套壳。共用 UI、客户端状态和 API 模型，平台能力分开实现。降低长期开发成本是目标，不能当成已量化验证的收益。
- Go 是账本、去重、分录和统计规则的唯一实现。采用模块化单体，不提前拆微服务。
- 一个本机 Go 服务提供 Web 和 API，Web / Mac 共用 SQLite。后续云端候选为 PostgreSQL，尚未实现远程部署或同步。
- 兼容性优先：稳定 UUID、整数分/字符串金额、实体版本、schema 版本与事务内 change_log；保留这些边界，不把复制数据库当作云端同步。
- 目标来源是支付宝、微信、京东白条及招商、农业、上海银行、工商银行。卡种、格式和脱敏样本仍待确认。
- 希望减少日常人工观察：指定文件夹自动归集优先，邮件附件随后，必要时手动导出补充；手动记账用于补漏。付款后自动记账是需真机验证的后续增强，不是当前承诺。
- 初始超管通过私有配置指定，Google 和 GitHub 分别绑定稳定身份；同一人的两个入口不自动合并为一条身份。白名单成员共同读写当前账本，不是多租户隔离。

## 产品表达原则

MVP 靠可用功能成立，不以页面文字填充未实现的能力。界面只保留操作、结果、错误和影响决策的必要说明。

- 删除账本页宣传段落、Web/Mac/KMP 等实现信息、正常连接横幅、重复的本机保存提示。
- 品牌 Slogan 原文保持 `Know Your Money. Own Your Future.`，仅放登录品牌入口；应用窗口及网页标题为 Monee。
- 格式帮助、数据与隐私、账号 ID/备注、操作记录按需展开；不把内部 actor 标识直接作为操作人名称。
- 保留重复入账确认、不支持交易类型、权限限制和数据保存/备份边界。简化表达不能掩盖财务风险或制造已同步的假象。
- 正式导入页只有空模板；合成示例在测试资料中。功能未实现就不显示占位洞察、伪数据或泛泛建议。

## 已实现与未实现

| 领域 | 已实现 | 尚未实现 |
| --- | --- | --- |
| 账本 | 标准 UTF-8 CSV 预览/批次确认、重复和冲突检查、手动人民币收入/支出、持久化 | 原生平台/银行格式、编辑、撤销、退款、转账、还款、可靠资产余额 |
| 查询 | 月度收支、分类分布、中文搜索、分页、行内详情、定时刷新 | 来源覆盖分析、变化解释、主动提醒、大规模性能验收 |
| 获取 | 选择文件或粘贴标准 CSV | 目录监听、邮件接收、通知捕获、自动入账 |
| 权限 | Google/GitHub OAuth、后端白名单、超管成员管理和审计、退出与页面清理 | 多租户、只读角色、超管自助交接/恢复、邮件授权 |
| 桌面 | 品牌 ICNS、系统浏览器授权、proof 领取会话、自动回到前台、固定备用唤起链接 | Go 随包自动启动、签名、公证、更新、持久化登录 |
| 数据迁移 | 稳定标识与版本/change_log 基础 | 完整备份恢复、云数据库、同步协议、跨设备冲突处理 |
| 平台 | Web / Mac 工程与共享实现 | Android / iOS 工程及真机验证 |

## 验证证据与剩余缺口

- 2026-09-14：Go 全量 race 测试与 vet、KMP JVM 测试、Web 生产构建与 Mac .app 构建通过。另从 Git 基线导出仅含提交文件的干净目录复验通过，复用依赖及 Gradle 构建缓存；没有复制 OAuth 配置或个人数据库，不等于已完成另一台物理电脑的验收。具体命令见运行指南。测试身份提供方仅存在 `_test.go`，无生产免登录入口。
- 历史隔离账本验收已完成标准 CSV 导入、补记、两端读取一致、重复导入及重启持久化；没有向正式账本写入合成数据。
- GitHub：Web 和 Mac 真实登录通过，超管识别、白名单读取、退出回登录页已验证；自动回前台由用户确认。
- Google：真实 Web OAuth 凭据已配置在原开发机；已观察到 Mac 的 Google 超管登录、账本读取和首次邮箱绑定记录。Web 完整 Google 登录仍需单独验收，不能写成双端都通过。
- 文案调整后已检查 Mac 首页、导入/记账、白名单及折叠帮助/记录；Web 登录、隐私说明与白名单可见。退出后 Web 重载仍未登录。
- 备用 `monee://auth/complete` 的固定 URI、产物注册及安全测试通过，手动唤起的浏览器实测受原测试工具限制；最小化恢复未单独实测。
- Mac 输入法/文件选择完整体验、无障碍、更多浏览器、大列表与首屏性能仍待验收。应用 Wasm 约 3.21 MiB、Skiko 约 8.25 MiB、JS 约 340 KiB，中文字体约 17 MB；不是传输压缩量或耗时指标。

## 建议的接续顺序

以下是开发建议，不表示已经实施或新开任务。

1. 按运行指南在新目录跑通无凭据测试，再用自己的 OAuth 配置完成 Google Web 与 Mac 人工验收；确认本地 main 与远程一致。
2. 在扩大真实数据使用或新增 schema 迁移前，先补版本化备份/恢复及校验，避免以复制 SQLite 文件冒充完整恢复产品。
3. 用脱敏账单明确首个原生格式，做解析 → 预览 → 确认闭环；接入指定文件夹时复用该链路，验证重启补扫、稳定写入、失败重试及幂等。
4. 随后扩展邮件、其他来源与主动分析；移动端和付款通知捕获按真实设备能力单独验证。

需要产品负责人后续提供：各行卡种和脱敏格式样本、邮箱服务商与授权方式、异地密钥/账本备份的存储目的地。

## 已知接续陷阱

- 旧 Mac 进程不会自动加载新构建。当前分支重启后可从钥匙串恢复有效会话；旧版没有该能力。不能从浏览器或文件复制 session token 给它。
- Web 必须由 Go 同源提供；旧静态预览、旧分支和旧浏览器缓存可能显示演示页。先查实际分支、启动目录、监听进程和资源，再判断是否缺功能。
- `connection.json` 包含 baseUrl、instanceId、serviceProtocol；实例身份不是登录凭据。不要恢复旧的共享 token。
- `monee://auth/complete` 只唤起窗口，不作为 Google/GitHub 控制台回调，不带 token，也不接收任意 redirectTo。
- Google service-account JSON 不能用于个人登录。登录授权也不包含读取邮件的权限。
- 新建开发环境可以独立工作；Git 无法恢复原机的密钥、白名单数据库或真实账本，详见运行指南中的私有资料边界。

## 2026-09-15 旧版数据迁移适配

新增 Go `migrate-legacy` 命令，读取 ai-financial schema 1.0 部署快照，生成受当前 CSV 校验约束的普通消费候选、逐条处置 manifest 与完整源快照。不会修改正式账本、当前 schema 或认证；旧版退款/转账/还款及不确定收入留待核实。详见[功能差异、字段映射与运行方法](migration/ai-financial.md)。真实产物与原账单证据归档在仓库外私有目录，不提交 Git。

Go 全量 race 测试与 vet 通过；合成测试覆盖精度、退款隔离、稳定 ID、重试/冲突、行数及字节拆分、私有权限与禁止覆盖。本地旧库和快照逐条核对及迁移包数量/金额/哈希校验通过。没有改动 KMP，因此未重跑客户端构建；没有连接阿里云核对线上最新快照，也没有执行登录后的正式账本导入。

## 统一账号与账本授权（本次开发）

已实现稳定用户 UUID、多渠道绑定/确认合并、账号级准入、所有者/可编辑/只读、账号 ID 邀请并接受、账本隔离和共享 KMP 管理页面。正式数据库为 `application.db`；旧双库先备份再暂存升级，旧共享数据不自动授予所有白名单成员。架构与测试范围见[统一账号与账本](architecture/unified-accounts-ledgers.md)。飞书只预留扩展；当前仅本机部署，尚未上线阿里云。

历史导入已按用户批量校准收紧：同日同额同商户的全部候选成员转入核实清单，只有剩余普通人民币消费获准导入；真实数量、金额、账号与账本 ID 留在私有导入回执，不能进入 Git。

验收：Go 全量 race 测试与 vet、KMP JVM 测试、Web 生产构建和 Mac 分发构建通过。已在当前电脑用真实 GitHub OAuth 完成批准范围的导入，并验证重试不重复、逐月汇总、所有者和操作人关联；数据库备份独立恢复及完整性检查通过。真实账单、回执和备份均在 Git 外私有目录。浏览器自动化因管理员策略校验不可用未完成全面界面验收；阿里云部署仍属后续工作。


## 第一期 MVP 启动：导入基础切片（2026-09-15）

产品已批准四批交付：全来源现有格式接入 → 账目口径与纠错 → 自动处理 → 日常使用与备份。UI 随批次优化。需求与任务状态以 [Notion 产品工作台](https://app.notion.com/p/3dc0bbfd57ca8007a11cdeb45d807303) 为入口。来源包括支付宝、微信、京东白条及招商/农业/上海/工商银行，储蓄卡和信用卡按现有文件盘点，不凭空承诺所有导出格式。

本切片新增统一来源解析契约，并将标准 CSV 接到它上面；新增按账本授权的导入历史与来源明细只读 API。未升级数据库，未改正式数据，未改 UI。详见 [导入契约与当前边界](architecture/import-source-contract.md)。后续仍需落地独立来源证据持久化、原生格式、错误记录归档和 UI，不能将 M-02/M-04 的全部范围视为完成。

Go 全量 race 测试、vet 与服务构建通过，OpenAPI YAML 和差异检查通过。后续补验已完成共享 JVM 测试、Web 生产构建、Mac 分发构建及两端登录首页冒烟；三平台 CI 通过。登录后的两端导入链路仍待独立 OAuth 环境验收，详见 [PR #6 验证记录](operations/pr6-verification.md)。没有进行云端模型测试或付费调用。

用户接受程序先脱敏、云端 AI 处理，以及证据充分且校验通过后自动入账；疑问才需要确认。预算上限按月 50 元、单次历史任务 20 元、未来试跑 5 元设计，本轮明确不做模型试跑。指定 DeepSeek V4 Flash；官方当前映射到 V4.1 Flash 的情况已告知。费用控制与模型集成尚未实现，凭据不进入仓库或 Notion。

## ai-ledger 参考分析与支付宝来源解析（2026-09-15）

已只读分析用户指定的私有 ai-ledger 仓库。该仓库只有代码、规则和聚合资料，原始文件、SQLite 与逐笔快照被 Git 忽略；不能据此恢复历史账单。通用格式盘点、旧规则风险和后续接入顺序见 [参考分析](architecture/ai-ledger-assessment.md)，私人聚合结论未复制进本仓库。

新增支付宝 UTF-8/GB18030 来源提取与 `inspect-bill` 私有解析包命令，保留状态、原始列、两类订单号和来源位置；不连接数据库，不自动入账，尚未接 Web/Mac 导入入口。Go 全量 race 测试、vet、所有命令构建、共享 JVM 测试和 Web/Mac 分发构建通过。真实原始文件尚不可用，未宣称真实格式验收完成。两端实际操作验收仍待完成；继续时电脑已锁屏，需要用户手动解锁。没有云端模型调用。
